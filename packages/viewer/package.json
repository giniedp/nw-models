{
  "name": "@nw-models/viewer",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm vite serve",
    "build": "pnpm vite build"
  },
  "nx": {
    "targets": {
      "build": {
        "outputs": [
          "{workspaceRoot}/dist/viewer"
        ]
      }
    }
  }
}
