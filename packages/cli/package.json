{
  "name": "@nw-models/cli",
  "version": "1.0.0",
  "scripts": {
    "dev": "tsc -w",
    "build": "tsc -p ./tsconfig.json",
    "test": "globstar -- node --import tsx --test \"./**/*.spec.ts\""
  },
  "nx": {
    "targets": {
      "build": {
        "outputs": [
          "{workspaceRoot}/dist/cli"
        ]
      }
    }
  }
}
